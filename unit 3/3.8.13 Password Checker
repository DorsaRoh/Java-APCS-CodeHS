public boolean passwordCheck(String password)
    {
        if(password.matches("[A-Za-z0-9]+") && password.length() >= 8)
        {
            return true;
        }
        
        return false;
    }
